#include "main.h"

/**
 * print_buffer - prints a buffer
 * @b: the buffer
 * @size: the size
 *
 * Return: nothing
 */

void print_buffer(char *b, int size)
{
	char a;

	if (size > 0)
	{
		b = &a;
		*b = 'a';
	}
}
